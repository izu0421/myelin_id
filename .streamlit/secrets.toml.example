# Copy to secrets.toml and fill in a GitHub Personal Access Token with
# `contents: read/write` permission on the target repo. Never commit the real token.
[github]
token = "github_pat_your_token_here"
